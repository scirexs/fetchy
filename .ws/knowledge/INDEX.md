# Knowledge Index

## Language & Types

## Framework & Libraries

## Build, Tooling & Dependencies
- github-packages-npm-no-provenance.md — GitHub Packages' npm registry needs per-scope registry setup and rejects `npm publish --provenance`.
- dnt-output-registry-agnostic.md — dnt's `./npm` build output is registry-agnostic; publish it unchanged to multiple npm-compatible registries instead of rebuilding.

## Testing

## Runtime & Platform

## Integration & Data

## Other
